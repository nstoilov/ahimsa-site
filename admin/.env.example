VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Media CDN (R2 custom domain). Public read URLs for cover images/audio/video.
VITE_MEDIA_BASE_URL=https://media.ahimsaapp.com
# Supabase Edge Function that presigns R2 PUT URLs for admin uploads.
VITE_R2_PRESIGN_URL=https://zskpabenylubupwvaavh.supabase.co/functions/v1/r2-presign

# ---------------------------------------------------------------------------
# R2 S3 credentials (NOT client vars — never use a VITE_ prefix here).
# Apply these as Supabase Edge Function secrets:
#   supabase secrets set --project-ref zskpabenylubupwvaavh \
#     R2_ACCOUNT_ID=... R2_ACCESS_KEY_ID=... R2_SECRET_ACCESS_KEY=... R2_BUCKET=ahimsa-media
# or: supabase secrets set --project-ref zskpabenylubupwvaavh --env-file admin/.env.functions
# ---------------------------------------------------------------------------

export default {
  api: {
    projectId: process.env.SANITY_STUDIO_PROJECT_ID || 'cqeu4nnj',
    dataset: process.env.SANITY_STUDIO_DATASET || 'production',
  },
  studioHost: 'ahimsa-blog',
  autoUpdates: true,
}

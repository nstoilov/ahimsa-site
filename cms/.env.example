# Optional overrides for the Sanity Studio — the project ID and dataset are
# baked into sanity.config.js / sanity.cli.js, so copying this file is only
# needed to point the studio at a different project/dataset.
SANITY_STUDIO_PROJECT_ID=
SANITY_STUDIO_DATASET=production
